# Viewer Analysis Skill

Use this recipe when the task needs a local, read-only browser view over AgentPlane task artifacts.

Prefer the viewer for inspection and reporting work. Do not select it when a local HTTP listener is
not allowed or when the task requires mutating task state.
